---
- name: "Prepare target hosts"
  hosts: "all"
  tasks:
    - name: "Update apt cache and install python3-debian"
      ansible.builtin.apt:
        name: "python3-debian"
        state: "present"
        update_cache: yes